# Проверки VESCHI AI

## Как обновить работающего бота

1. Распакуйте ZIP: файлы и папка `pages` лежат сразу в корне архива. Замените **одноимённые файлы в корне репозитория**, в том числе `tg_agent_new.py` и `pages/bot_worker.py`. Не загружайте ZIP в репозиторий как один файл и не создавайте в нём дополнительную папку `VESCHI_AI-main`.
2. Сохраните изменения (Commit changes) в ветке, из которой развёрнуто приложение Streamlit, и перезапустите приложение. Если бот запущен отдельной командой `python tg_agent_new.py`, перезапустите этот процесс. Не держите два процесса polling для одного Telegram-бота.
3. Отправьте боту `/version`. Ответ должен содержать `2026-09-25-r21`, имена `WB_TOKEN_1` и `WB_TOKEN_2` и пометку `задан` для каждого. Если версия другая, запущена старая копия кода.
4. После проверки версии отправьте «новинки», «остатки» и «аудит». Не публикуйте значения токенов и содержимое Secrets.

## Настройка

Установите зависимости из `requirements.txt` и задайте секреты окружения (в Streamlit Cloud: Settings → Secrets):

- `TELEGRAM_BOT_TOKEN` — токен Telegram бота;
- `WB_TOKEN_1`, `WB_TOKEN_2` — **разные** токены двух кабинетов, каждый с доступом к Контенту, Маркетплейсу (FBS) и Статистике. Старые названия `WB_API_TOKEN`, `WB_API_TOKEN_2` тоже поддерживаются, но названия `WB_TOKEN_1`, `WB_TOKEN_2` имеют приоритет;
- Форма отзывов и вопросов, а также почасовая проверка отзывов используют `WB_TOKEN_1`. Если у этого токена нет доступа к разделу «Вопросы и отзывы» WB, задайте отдельный `WB_FEEDBACK_TOKEN` с нужными правами. Токены не размещайте в файлах проекта;
- `MOYSKLAD_API_TOKEN` — токен МойСклад;
- `MOYSKLAD_STORE_NAME` — точное название склада, по умолчанию `МСК`;
- `SALES_DAYS` — период расчёта средней фактической продажи в календарных днях, по умолчанию `28`.

Запуск бота: `python tg_agent_new.py` либо кнопка на странице `bot_worker` в Streamlit. Не запускайте оба варианта одновременно: Telegram разрешает только один экземпляр polling для одного бота. Для непрерывной работы требуется постоянно работающий процесс; кнопка Streamlit запускает фоновый поток в рамках живого процесса приложения.

После запуска отправьте боту `/start` в том чате, куда должны приходить уведомления. Бот проверяет отзывы каждый час в 10 минут по Москве и сообщает только о новых ID. Первая проверка запоминает уже существующие отзывы. При HTTP 429 проверка пропускает час без повторных запросов. Для постоянных уведомлений нужен постоянно работающий процесс бота; при перезапуске состояние сохраняется в `telegram_feedback_state.json` при наличии доступа на запись. Можно указать `TELEGRAM_CHAT_ID`, если чат известен заранее.

В панели Streamlit появились вкладки «Отзывы» и «Вопросы покупателей». Загрузка вопросов по кнопке делает отдельный запрос WB. Черновики вопросов создаются с помощью `questions_prompt.md`, основанного на предоставленных примерах; публикация возможна только после ручного выбора и нажатия кнопки. Не проверяйте обе вкладки многократно во время действия лимита WB.

Для аудита не нужны файлы: отправьте боту «аудит». Бот читает карточки двух кабинетов через API WB и отправляет в Telegram артикулы с подтверждёнными ошибками ТН ВЭД или документов. Для всех предметов, кроме зонтов, требуется декларация соответствия типа 2 с номером и датами начала и окончания действия. Для зонтов проверяется отметка «документы не требуются». Проверка читает данные и не меняет карточки WB. Если API не отдаёт поле ТН ВЭД, документы либо категорию, бот присылает артикулы отдельным списком «Проверить вручную»; отсутствие поля в ответе нельзя считать отсутствием данных в личном кабинете.

В форме отзыва видны комментарий, плюсы и минусы. После нажатия публикации одобренный ответ сохраняется в Supabase и отправляется в WB. При HTTP 429 ответ остаётся в Supabase; GitHub Actions запускает проверку приблизительно дважды в час, после окончания периода ограничения WB. Telegram-бот по кнопке для отправки отзывов больше не нужен. При сетевой неопределённости, HTTP 4xx кроме 429 или HTTP 5xx очередь переходит в состояние «требует ручной проверки» без автоматического повторного POST. Если обработчик остановился во время отправки, запись остаётся «отправка начата» и требует ручной проверки в WB: повторное нажатие не отправит дубль.

## Команды

- `остатки`: складывает FBS всех складов каждого из двух кабинетов; сравнивает сумму с МСК и с семидневной потребностью на основе фактических продаж обоих кабинетов за `SALES_DAYS`. Не выставляет остатки автоматически. Продажи, а не заказы; возвраты не учитываются как продажи.
- `новинки`: показывает товар с наличием на МСК, только если карточка существует в кабинете WB, но её FBS остаток равен нулю. Проверяет каждый кабинет отдельно; в сообщении пишет только о кабинетах с существующей карточкой и нулевым остатком. Кабинеты без карточки не упоминает. Если во всех кабинетах, где карточка есть, остаток положительный, товар не выводится. Проверка не использует дату создания карточки.
- `/diagnostics`: выводит по каждому кабинету количество карточек, баркодов и число ненулевых остатков по каждому FBS складу. Используйте, если «новинки» показывает слишком много нулей; команда не показывает значения токенов.
- `/msdiagnostics`: выводит для каждого склада МойСклад количество товаров с положительным физическим остатком и суммарное количество штук. В анализе «новинки» и «остатки» учитывается только склад с точным названием `MOYSKLAD_STORE_NAME` (по умолчанию `МСК`); склад `МСК НЕТ НА СКЛАДЕ` исключается по ID и названию.
- `аудит`: проверяет опубликованные карточки двух кабинетов на ТН ВЭД и декларацию соответствия либо отметку для зонтов. Выводит все подтверждённые замечания несколькими сообщениями без ограничения в 30 артикулов. Черновики и карточки в корзине не входят в метод списка опубликованных карточек. Если WB не возвращает поле, бот перечисляет артикулы для ручной проверки; не называет эти поля пустыми.

Любой сбой источника данных останавливает проверку с сообщением об ошибке; отсутствие отклонений выводится только после успешного завершения запросов. Если отчёт МойСклад не содержит артикул в строке, скрипт ищет его в карточке товара МойСклад. Товары без артикула выводятся отдельным предупреждением и не останавливают сверку остальных товаров. Результат по ним остаётся непроверенным.
